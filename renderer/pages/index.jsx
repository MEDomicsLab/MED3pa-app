import React from "react"

/**
 * Next.js requires a page at the route root. The whole application is rendered
 * from `_app.js` (see `components/shell/appShell.jsx`), so this page renders
 * nothing on purpose.
 */
export default function Home() {
  return null
}
