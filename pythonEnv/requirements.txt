# MED3pa standalone app — python requirements
#
# Verified working set on Python 3.12 (see pythonEnv/create_conda_env.sh).
#
# Python 3.12 is REQUIRED, not merely supported: MED3pa pins checkpointer==2.1.0,
# and every checkpointer 2.x release up to 2.6.1 declares Requires-Python >=3.12.
# On 3.11 the install fails outright with "No matching distribution found".
#
# The MED3pa library is NOT on PyPI. Install it from GitHub (below) or, when
# developing against a local checkout, replace that line with an editable install:
#     pip install -e ../packages/MED3pa
MED3pa @ git+https://github.com/MEDomicsLab/MED3pa.git@639b9c8fa8e249af3cf2613bf806e34bfcc10534

# --- MED3pa's own dependencies, pinned to the combination that actually resolves ---
# numpy is held at 1.26.4 because MED3pa requires <2.1.0. That in turn forces
# scipy: installing MED3pa unpinned pulls scipy 1.18, which requires numpy>=2.0
# and breaks the install. scipy 1.11.4 is the last line that is happy on numpy 1.26.
numpy==1.26.4
scipy==1.11.4
pandas==2.3.3
scikit_learn==1.9.0
matplotlib==3.9.2
checkpointer==2.1.0
relib==1.2.0
ray==2.56.1
torch==2.12.1
xgboost==2.1.1
PyYAML==6.0.3
tqdm==4.70.0
setuptools==69.5.1

# --- needed by this app's own python layer ---
pymongo==4.7.3          # med_libs/mongodb_utils.py, results persistence
onnxruntime==1.28.0     # med_libs/model_loading.py, ONNX base models
joblib==1.5.3           # pickle/joblib base models
